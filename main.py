from Solutions import one, one_two, two, two_two, three
from utils import timerun, strReplace

if __name__ == '__main__':
    #timerun(one)
    #timerun(one_two)
    #timerun(two)
    #timerun(two_two)
    timerun(three)

# See PyCharm help at https://www.jetbrains.com/help/pycharm/
